{
  "name": "music-composition-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    // Standard Create React App scripts
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    // Ensure you're on a compatible version of react-scripts (CRA 5 works well with Tailwind)
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",

    // Tailwind / PostCSS
    "tailwindcss": "^3.3.2",
    "postcss": "^8.4.21",
    "autoprefixer": "^10.4.14"
  },
  "browserslist": {
    // Standard config for CRA + Tailwind
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
